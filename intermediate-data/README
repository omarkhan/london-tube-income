This directory contains intermediate data saved during the data processing
phase. It is saved here to make it quicker to rerun the latter stages of data
processing, and because the data might be useful for other projects.
